import type { VwsMetierProfile } from "./types";

/** Tous les métiers VWS d’origine sont migrés — tableau vide conservé pour compat imports. */
export const LEGACY_PENDING_METIER_PROFILES: VwsMetierProfile[] = [];

export const LEGACY_EXCLUDED_LABELS = new Set<string>();
