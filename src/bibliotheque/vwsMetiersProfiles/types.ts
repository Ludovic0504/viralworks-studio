export type VwsMetierProfile = {
  /** Libellé affiché (clé = campaign.profession) */
  label: string;
  environmentHint: string;
  stylePlaceholder: string;
  inspireContext?: string;
  recommendedVideoFormatId: string;
};
